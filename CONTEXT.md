# oh-grasp — 单文件结构图增强服务

一个轻量、零第三方依赖的「增强服务」项目：给**单个代码文件**生成一张自包含 HTML 结构图（外部依赖、内部模块、数据流、输入输出、文件作用），帮助用户快速建立对陌生大文件的认知。装进 Claude Code 等 agent 平台，靠平台自身的模型去执行。受 Archify 启发，但独立实现。

## Language

**文件结构图 (File structure diagram)**:
本增强服务的唯一产出：针对单个代码文件生成的自包含 HTML 图，展示外部模块（一个 import 即一个）、内部模块及其数据流，并辅以输入/输出/作用说明。**中英双语**，可在图内切换。

**JSON IR (JSON 中间表示)**:
模型分析文件后产出的结构化数据（针对单文件的裁剪 schema：外部模块、内部模块、分组、连接、摘要，散文双语）。是校验脚本的输入、viewer 渲染的数据源；取代了 Mermaid。

**增强服务 (Enhancement)**:
一个自包含、零第三方依赖的包（prompt + Node 标准库脚本 + viewer 模板），装进 agent 平台后，给该平台的模型新增一项它原本不擅长的领域能力。
_Avoid_: skill（与 Claude Code 的 Skill 机制撞名）、plugin（与 DSH 插件撞名）

**适配器 (Adapter)**:
把一份增强服务映射到某个平台原生机制的薄层。Claude Code 上落地为 Skill，将来 DSH 上落地为插件。

**校验脚本 (Validation script)**:
用 Node 标准库写的脚本，校验 JSON IR 是否满足 schema（字段齐全、引用有效）与存在性（内部模块在文件中真存在），返回「过/不过 + 错在哪」，供模型自纠。

**查看器 (Viewer)**:
把 JSON IR 渲染成自包含 HTML 的轻量模板，从零实现、仅借鉴 Archify 的输出形态。无 CDN 依赖。整个产物**只有一个界面**（画布 + 统一弹窗 + 侧栏），不存在视图切换。

**双语 IR (Bilingual IR)**:
IR 里的**散文**字段（说明、描述、标签、四段）存为 `{zh, en}` 两个语言，语言在外层；**名字**字段不译——内部模块的 `label` 是源码标识符、外部模块的 `label` 是包名，译了就对不上代码。中英两种语言**都必填**，缺一即校验不过。

**四段说明 (Four-part description)**:
每条 connection 上对「这条线上流的是什么」的固定四字段说明：`source`（从哪来）/ `process`（经过什么处理）/ `output`（输出了什么）/ `purpose`（用于什么），中英各一套，每段一句。
_Avoid_: 写成「边注释」——注释是图上的文字，四段说明是 IR 里的数据，画布中点只放它的一句摘要。

**消费关系 / 宿主调用 (uses / runtime)**:
两类**不属于数据流**的依赖事实，都记在内部模块上。`uses` = 该模块直接消费了哪些外部模块（引用 external 的 id）；`runtime` = 该模块对宿主运行时的调用（如 `process.exit`、`process.env`，只写属性路径）。外部依赖**不进** `connections`。

**虚节点 (Virtual node)**:
长边（跨层 ≥ 2 的前向边）在相邻层之间的空隙处插入的无实体折点，只用于让线段逐层直角穿下、不穿盒体。参与层内排序，但不渲染。
_Avoid_: 与「端口圆点」混为一谈——端口是真实节点边缘上的对接点，虚节点是边自己的路径上的点。

**端口口径 (Port count)**:
端口旁 `×N` 里那个 N 的含义：**不同内容的种数**（按 `zh` 文本去重），不是连接条数。代表内容取 IR 顺序第一条，画在 `×N` 右侧；空方向显式标「—」，不隐藏。

**算法文档 (Algorithm doc)**:
`docs/algorithms/` 下的现状文档，按问题域分文件，记「用了什么算法、解决什么问题、复杂度多少、代码在哪」。与 ADR 的分工：ADR 记**为什么这么选**，算法文档记**现在跑的是什么**。

**零依赖 (Zero-dependency)**:
指零第三方包；仅依赖平台/机器自带的 Node 标准库。viewer 亦自包含（无外部字体/CDN）。是贯穿本项目的硬约束。
