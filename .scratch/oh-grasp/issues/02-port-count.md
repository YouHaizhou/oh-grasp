# 02 — 端口说人话：`×N` = 内容种数 + 代表内容 + 空方向「—」

**What to build:** 端口上那个数字，从「连接条数」改成「**不同内容的种数**」——点开数得清，跟图上的数字对得上。数字右侧直接显示**代表内容**（取 IR 顺序第一条，盒内、`fitWidth` 截断、**盒高不变**）。**空方向不再隐藏**，显式渲染「—」，让「这个方向没有数据」和「图漏画了」分得开。顺带把入边端点从盒的上边缘外移，让箭头不再被后画的盒体/端口圆点盖住。

**Blocked by:** 01（`×N` 去重按 `zh` 文本，得先有 `zh` 字段）

**Status:** ready-for-agent

- [ ] `×N` 的 N = 该方向上**不同内容种数**，去重按 `zh` 文本
- [ ] **切换语言后 N 不变**（最容易写错成按当前显示语言去重——单独测这条）
- [ ] 代表内容取 IR 顺序第一条，画在盒内 `×N` 右侧，用 `fitWidth` 截断
- [ ] **盒高不变**（布局算法的输入不变——行几何 / 层距 / 区域堆叠都不动）
- [ ] 空方向显式渲染「—」，不再是「不画端口」
- [ ] 入边端点外移约 8px，箭头落在端口圆点正上方可见；**不动层序**（节点仍盖住边端点）
- [ ] **补一份带 group 的 fixture**——现有 fixture 是 5 modules / 3 connections / **0 groups**，而本票的行为全是 group 才有的，目前零覆盖
- [ ] `docs/algorithms/graph.md` 里 `countPorts` 一条同步更新（改了函数就改文档，行号是快照、以函数名为准）

**参考**：ADR-0009；spec User Stories 24–27；`docs/algorithms/graph.md` 的 `aggregateEdges` 折叠 bug 记录（历史上曾因此让 `openPort` 失效）。
