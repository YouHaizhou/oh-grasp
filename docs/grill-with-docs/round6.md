诊断（facts，我的活）

  archify 的 IR：41 个内部模块、43 条边、4 层。最宽的是第 0 层 18 个节点，需要 4640px 宽。

  1. 图显示不完全 → viewer.js:75 把 SVG 宽度写死 W = 720，viewer.js:81 用 x = (W - total) / 2 把节点居中，viewer.js:85 viewBox="0 0 720 H"。第 0 层 18
  个节点铺开要 4640px，居中后从 x=-1960 起，超出 viewBox 的部分被 SVG 内建裁掉。缩放/平移只是 CSS transform，改变不了 viewBox
  的裁剪——所以你观察「不是缩放问题」是对的。
  2. 源码只显示函数声明 → 数据问题，非渲染。IR 里每个 internal 的 source 平均 47 字符、最长 125，全是 function usage() {
  这种声明行。根因在契约：prompt.md 和 schema.json 都写「源码/签名」——「签名」二字给了模型只给声明的许可。
  3. 连线乱 → 同一根因。18 节点挤一层横向铺 4640px，43 条边用朴素贝塞尔 + 无交叉最小化、无边路由、边标签固定偏移，节点多且宽就必然打结。

  三个 bug 全在 viewer + 数据契约 这一层，都跟「分析粒度」无关。下面开始 grill。

  ---
  🔥 Round 1 · 3 questions

  🎯 Q1 - 修复范围（根决策）
  三个 bug 都在 viewer / 数据契约层，但背后还藏着一个更大的产品问题：41 个扁平函数是不是「本来就不该铺 41 个节点」。
  - (a) 只修 viewer + 数据契约：让 41 节点正确渲染（宽高自适应 + fit）、修 source 契约。不动分析粒度。
  - (b) 同时改分析侧：让模型把扁平函数聚合成 5–15 个逻辑模块（改 IR 加「分组/容器」概念，动 prompt + schema + validate +
  viewer，且会跟「label=真实标识符」的存在性校验冲突）。
  - (c) 两者都做。
  💡 推荐 (a)。三个 bug 互不依赖分析粒度，先修掉让任何大文件都能正确出图；分组是更大的契约改动、有自己的设计空间，值得单独一轮 grill。
  ans：a。

  ⚖️  Q2 - 布局怎么修
  最宽层 4640px vs 固定 720px，怎么解决可见性 + 连线乱？
  - (a) 最小修复：SVG 宽高按节点包围盒自适应，初始 fit-to-view，保留滚轮缩放/拖拽/双击复位。
  - (b) 换布局算法（力导向 / Sugiyama + 交叉最小化），更漂亮但是大工程。
  - (c) 横改竖：层内节点纵排、层间横排，牺牲「分层流向」换省空间。
  💡 推荐 (a)。先让 41 节点全部可见、初始就装进视口，这是硬 bug 的最小解；算法升级独立成事，别绑在一起。
  ans：a.

  🧩 Q3 - source 字段到底存什么
  现在模型钻「源码/签名」的空子只给声明行。改成？
  - (a) 完整函数体（原文摘出）+ 起始行号 —— 弹窗能看完整代码，代价 HTML 膨胀（41 函数≈复制整个 1991 行文件）。
  - (b) 签名 + 关键几行摘录 —— 折中。
  - (c) 维持签名 only，但把字段名/契约改清楚（改成「签名 signature」），去掉误导。
  💡 推荐 (a)。目标是「点一下看懂陌生函数」，完整源码最有价值；离线单文件膨胀可接受。若采纳，顺手让 validate 校验「source 与源文件该行处原文一致」。
  ans：a